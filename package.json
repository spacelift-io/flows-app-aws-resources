{
  "type": "module",
  "dependencies": {
    "@slflows/sdk": "^0.2.0",
    "@aws-sdk/client-cloudcontrol": "^3.0.0",
    "@aws-sdk/client-cloudformation": "^3.0.0",
    "typescript": "^5.8.3"
  },
  "devDependencies": {
    "@types/node": "^24.0.7",
    "@useflows/flowctl": "^0.1.1",
    "prettier": "^3.6.2",
    "ts-node": "^10.9.2",
    "tsx": "^4.0.0"
  },
  "scripts": {
    "setup": "./setup.sh",
    "bundle": "npx flowctl version bundle -e main.ts",
    "format": "npx prettier --write .",
    "typecheck": "npx tsc --noEmit",
    "generate": "npx tsx scripts/generateSchemas.ts && npx prettier --write ."
  }
}
